package com.tintuna.stockfx.controller;

import java.net.URL;
import java.util.ResourceBundle;

import javafx.fxml.Initializable;
import javafx.scene.Node;
import javafx.scene.control.Tab;

public class Application implements Initializable {

//	private MainController mainController;
//	private TabManager tabManager;

	public Application() {
		
	}

	//
	public void initialize(URL arg0, ResourceBundle arg1) {
//		mainController = new MainController();
//		tabManager = new TabManager(mainController.getTabPane());
//		Tab mainTab = new Tab("Portfolios");
////		mainTab.setContent(((Node) mainController).getParent());
//		System.out.println("mainController.getTabPane():" + mainController.getTabPane());
//		mainController.getTabPane().getTabs().add(mainTab);
//
//		tabManager.addNewDocument("one", null, null);
//		tabManager.addNewDocument("two", null, null);
//		tabManager.addNewDocument("three", null, null);
	}
}
